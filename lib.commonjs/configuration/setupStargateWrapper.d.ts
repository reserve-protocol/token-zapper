import { type Universe } from "../Universe";
export declare const setupStargateWrapper: (universe: Universe, vaultAddr: string[], wrappedToUnderlyingMapping: Record<string, string>) => Promise<void>;
